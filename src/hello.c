/*primeiro arquivo linguagem C.
para iniciar iremosimportar a biblioteca 
stdio (std-standar(padrão)
io-input output
(entrada e saida)).
esta biblioteca possui os comandos para interação com o teclado e monitor*/
#include <stdio.h>

/*iniciar a estrutura do codigo para a 
mensagem hello world, usando a função main (principal),
pois o compilador C ira procurar esta função da dar execução no nosso codigo.*/
int main(){
 printf("Hello world!\n");
 return 0;
}